<nav class="navbar navbar-default navbar-static-top">
  <div class="container">
    <a><%= link_to("Jongno Language Exchange", root_path, class: "navbar-brand") %></a>
      <ul class="nav navbar-nav navbar-right">
        <li>
          <p class="navbar-btn">
            <% if user_signed_in? %>
              <a><%= link_to("edit schedule", dashboard_path, class: "btn btn-primary outline small") %></a>
              <a><%= link_to("log out", destroy_user_session_path, method: :delete, class: "btn btn-primary outline small") %></a>
            <% else %>
              <a><%= link_to("log in", new_user_session_path, class: "btn btn-primary outline small") %></a>
            <% end %>
          </p>
        </li>
      </ul>
  </div>
</nav>
